# Sets tab id if not provided

Sets tab id if it wasn't provided

## Usage

``` r
set_tab_id(tab)
```

## Arguments

- tab:

  A tab. Tab is a list of three elements - first element defines menu
  item, second element defines tab content, third optional element
  defines tab id.
